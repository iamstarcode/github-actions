name: First Workflow

on:
  push:
    branches: [main]
jobs:
  first-job:
    runs-on: ubuntu-latest
    name: First Job
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Run a one-line script
        run: echo Hello, world!
